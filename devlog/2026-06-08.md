# 开发日志 — 2026-06-08

## 已完成

- [x] 项目需求确认（与用户沟通，明确 5 个维度的技术选型与功能范围）
- [x] 项目管理基础设施搭建（Phase 0）
  - 创建 CLAUDE.md 工作指南
  - 创建 docs/requirements.md 需求文档
  - 创建 docs/tech-stack.md 技术栈说明
  - 创建 docs/design-spec.md 设计规范
  - 创建 docs/architecture.md 架构设计
  - 创建 docs/execution-steps.md 执行步骤
  - 创建 devlog 开发日志目录及今日日志

- [x] Phase 1：项目脚手架（Electron + React + Vite + TypeScript + Tailwind）
  - 创建 package.json，安装 503 个依赖包
  - 配置 tsconfig.json（src） + tsconfig.node.json（electron）
  - 配置 vite.config.ts（React + Tailwind CSS 插件）
  - 创建 electron/main.ts（窗口 360×500，暗色背景，dev/prod 模式）
  - 创建 electron/preload.ts（contextBridge 壳）
  - 创建 scripts/dev.mjs（一键启动 Vite + Electron）
  - 创建 index.html + src/main.tsx + src/App.tsx + src/index.css
  - 创建 .gitignore
  - ✅ TypeScript 类型检查通过（src + electron 两端）
  - ✅ Vite 构建成功（753ms）
  - ✅ Vite 开发服务器启动正常（398ms，localhost:5173）

- [x] Phase 2：核心计时逻辑
  - 创建 src/types/index.ts（TimerPhase, Task, PomodoroRecord, AppSettings, TimerState）
  - 创建 src/stores/timerStore.ts（Zustand 状态机）
    - 时间戳差值法：rAF 每帧计算 `剩余 = 总时长 - (now - startTime)`
    - 状态流转：idle → work ⇄ shortBreak → work → longBreak → work（循环）
    - 完成 4 个工作番茄后触发长休息，长休后周期计数器归零
    - skip 不计数，pause/resume 通过反向推算 startTime 保持精度
  - 创建 src/hooks/useTimer.ts（rAF 驱动 tick，组件挂载时启动单次循环）
  - 更新 App.tsx 显示可操作的倒计时界面（开始/暂停/跳过 + 阶段标签 + 番茄计数）
  - 重构 getNextPhase：移除内部 +1，改为调用方显式传入完成后的计数，skip 不再需要手动 -1
  - ✅ TypeScript 类型检查通过，Vite 构建成功（35 modules, 749ms）

- [x] Phase 3：计时器 UI
  - 创建 src/components/timer/CountdownDisplay.tsx
    - 等宽数字 `font-mono tabular-nums`，8xl 字号
    - 工作阶段白色，休息阶段绿色 (#43a047)，最后 5 秒橙色脉冲 (#ff9800)
    - 休息阶段背景绿色光晕效果
    - idle 状态显示引导文字
  - 创建 src/components/timer/PhaseIndicator.tsx
    - 圆角药丸标签：工作红底、休息绿底、idle 灰底
    - 番茄点阵（4 个圆点，完成几个亮几个，红色发光）
    - 今日完成计数 🍅N
  - 创建 src/components/timer/TimerControls.tsx
    - 圆形主按钮：运行中显示暂停 ⏸，停止时显示开始 ▶
    - 跳过按钮：仅非 idle 时显示，次要样式
  - 创建 src/components/layout/AppLayout.tsx（主内容垂直居中壳）
  - 重构 App.tsx：从单文件拆分为 5 个组件的组合
  - ✅ TypeScript 编译通过，Vite 构建成功（39 modules, 705ms）

- [x] Phase 4：系统托盘
  - 创建 electron/tray.ts
    - 程序化生成 16×16 番茄红圆形托盘图标（BGRA 像素缓冲）
    - 托盘右键菜单：显示窗口 / 退出
    - 单击托盘图标：切换窗口显示/隐藏
  - 重构 electron/main.ts
    - 关闭窗口 → hide() 隐藏到托盘（不退出）
    - 添加 isQuitting 状态区分"关闭窗口"与"真正退出"
    - 注册 IPC 处理器：window:minimize-to-tray, app:get-version
  - 更新 electron/preload.ts：暴露 ElectronAPI 类型化接口
  - 创建 src/types/electron.d.ts：全局 Window.electronAPI 类型声明
  - ✅ TypeScript 编译通过（electron + src 两端）
  - ✅ Vite 构建成功（39 modules, 730ms）

## 待办

- [ ] Phase 5：通知系统
- [ ] Phase 6-9：见 execution-steps.md

## 遇到的问题

- npm install 因系统缓存目录权限问题失败，改用 `--cache` 指定项目本地缓存目录解决
- tsconfig.node.json 需添加 `esModuleInterop: true` 以兼容 Electron CommonJS 模块导入

## 技术决策记录

- 状态管理选 Zustand（轻量，够用）
- 持久化选 electron-store（JSON 文件，简单可靠）
- 计时精度采用时间戳差值法（避免 setInterval 后台节流）
- 默认暗色主题，窗口尺寸 360×500px
